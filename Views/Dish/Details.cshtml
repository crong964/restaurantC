@model _netmvc.Models.Dish

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Movie</h4>
    <hr />
    <dl class="row">
        <dt class="col-12 my-2">
            Tên món ăn: @Html.DisplayFor(modelItem => modelItem.Name)
        </dt>
        <dd class="col-12">
            <img class="object-fit-cover rounded" style="height: 200px; width: 200px;" src="/sta/@Model.PathImage"
                alt="" srcset="">
        </dd>
        <dt class="col-12">
            @ViewData["TypeName"]
        </dt>
        <dd class="col-12">
          Giá:  @Html.DisplayFor(model => model.Price)
        </dd>
    </dl>

    <form asp-action="Details">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
